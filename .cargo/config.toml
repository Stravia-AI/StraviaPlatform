[build]
# 不依赖开发机上可能已卸载的全局编译器包装器；需要缓存时可显式设置 RUSTC_WRAPPER。
rustc-wrapper = ""

# 使用 Rust 工具链自带的 LLD 缩短开发构建的链接时间，同时保留完整 PDB。
[target.x86_64-pc-windows-msvc]
linker = "rust-lld"

[target.aarch64-pc-windows-msvc]
linker = "rust-lld"
