[package]
name = "stravia-vendor-management-contract-fixture"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
semver = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
stravia-vendor-sdk = { path = "../.." }

[workspace]
